# Auto-loaded by `docker compose` alongside docker-compose.yml for local
# dev -- exposes ports directly on the host, skipping Caddy/TLS.
services:
  web:
    ports:
      - "3000:3000"

  postgrest:
    ports:
      - "3001:3000"
