{
  "name": "fidata",
  "private": true,
  "scripts": {
    "dev": "docker compose -f docker-compose.yml -f docker-compose.local.yml up --build postgres postgrest web",
    "dev:stop": "docker compose -f docker-compose.yml -f docker-compose.local.yml stop postgres postgrest web"
  }
}
